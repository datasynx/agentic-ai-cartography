# status

Show the current project status:

1. Tasks completed vs. open (`docs/tasks.md`)
2. TypeScript errors (`npm run lint 2>&1 | tail -20`)
3. Test status (`npm run test 2>&1 | tail -20`)
4. Recommendation: next logical step
