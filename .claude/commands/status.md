# status

Zeige den aktuellen Projekt-Status:

1. Tasks erledigt vs. offen (`docs/tasks.md`)
2. TypeScript-Fehler (`npm run lint 2>&1 | tail -20`)
3. Test-Status (`npm run test 2>&1 | tail -20`)
4. Empfehlung: nächster sinnvoller Schritt
